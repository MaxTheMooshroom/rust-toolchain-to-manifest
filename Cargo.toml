[package]
  name = "toolchain-to-manifest"
  version = "0.2.0"
  edition = "2024"

[profile.release]
  panic = "abort"
  strip = true
  opt-level = "z"
  lto = true
  codegen-units = 1

[dependencies]
  clap = { version = "4.6.1", features = ["derive"] }
  rustup-toolchain-manifest = "0.3.0"
